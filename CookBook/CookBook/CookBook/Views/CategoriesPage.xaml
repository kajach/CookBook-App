<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:CookBook.Models"
             x:Class="CookBook.Views.CategoriesPage"
             BackgroundColor="{StaticResource PageBackgroundColor}"
             Title="Categories">
    <StackLayout Style="{StaticResource MenuStyle}">
        <!--Menu Header-->
        <StackLayout Style="{StaticResource MenuHeaderStyle}">
            <Image Source="icon.png"
                   HeightRequest="50"/>
            <StackLayout>
                <Label Text="YACA" 
                       Style="{StaticResource MenuTitleStyle}"/>
                <Label Text="Yet Another Cookbook App" 
                       Style="{StaticResource MenuSubTitleStyle}"/>
            </StackLayout>
        </StackLayout>
        <!--Menu List-->
        <ListView SelectedItem="{Binding SelectedMenuItem}"
                  Style="{StaticResource MenuListStyle}">
            <ListView.ItemsSource>
                <x:Array Type="{x:Type models:MainMenuItem}">
                    <models:MainMenuItem Title="Home" Icon="icon.png" PageName="HomePage" />
                    <models:MainMenuItem Title="Search" Icon="icon.png" PageName="CategoriesPage" />
                    <models:MainMenuItem Title="Categories" Icon="icon.png" PageName="CategoriesPage" />
                    <models:MainMenuItem Title="Tags" Icon="icon.png" PageName="RecipeListPage" />
                    <models:MainMenuItem Title="Shopping Lists" Icon="icon.png" PageName="RecipeListPage" />
                    <models:MainMenuItem Title="All Recipes" Icon="icon.png" PageName="RecipeListPage" />
                    <models:MainMenuItem Title="Settings" Icon="icon.png" PageName="RecipeListPage" />
                </x:Array>
            </ListView.ItemsSource>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Style="{StaticResource MenuItemStyle}" BackgroundColor="Aqua">
                            <Image Source="{Binding Icon}"
                                   HeightRequest="35" />
                            <Label Text="{Binding Title}"
                                   Style="{StaticResource MenuTextStyle}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>